
zadanie_ot_10_06_2022_Yury

# 1) ѕользователь вводит сторону квадрата, необходимо
# рассчитать площадь квадрата, периметр и длину диагонали,
# после чего вывести это все на экран.


print ('¬ведите сторону квадрата')
a = int (input ())

print ('ѕлощадь квадрат =', int(a*a))
print ('ѕериметр квадрат =', int(4*a))
print ('ƒлина диагонали =', float(round(((2 ** 0.5)*a) , 4)))

# 2) ѕользователь вводит три стороны треугольника,
# необходимо по формуле √ерона должны рассчитать
# его площадь и вывести на экран.

print ('¬ведите сторону треугольника a')
a = int (input ())

print ('¬ведите сторону треугольника p')
k = int (input ())

print ('¬ведите сторону треугольника c')
c = int (input ())

p = float (a + k + c)/2

print ('ѕлощадь треугольника =', float(round(((p*(p-a)*(p-k)*(p-c))**0.5) , 4)))


# 3) ѕользователь вводит  трехзначное число. Ќеобходимо найти
# сумму цифр и вывести ее на экран.


print ('¬ведите трехзначное число')
a = int (input ())
summ = 0
if a > 0:
    z = a % 10
    summ = summ + z
    a = a // 10
    if a > 0:
        z = a % 10
        summ = summ + z
        a = a // 10
    if a > 0:
        z = a % 10
        summ = summ + z
        a = a // 10
print('—умма чисел =', str(summ))



# 4) ѕользователь вводит номер мес€ца (от 1 до 12), необходимо вы€снить
# к какому времени года принадлежит мес€ц и вывести на экран.

print ('¬ведите номер мес€ца от 1 до 12')
a = int (input ())
summ = 0

if 2 >= a > 0 or a  == 12:
    print ('Ёто зимний мес€ц')

elif 5 >= a >= 3:
    print ('Ёто весенний мес€ц')

elif 8 >= a >= 6:
    print ('Ёто летний мес€ц')

elif 11 >= a >= 9:
    print ('Ёто осений мес€ц')

